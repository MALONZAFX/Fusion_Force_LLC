[phases.setup]
nixPkgs = ["sqlite", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && gunicorn fusion_force.wsgi:application --bind 0.0.0.0:$PORT"